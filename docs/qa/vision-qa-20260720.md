# Vision QA 리포트 — 2026-07-20 (S1: 폴더 내 시크릿 생성)

> 스킬: `/vision-qa` · 테스트: `integration_test/vision_qa/s1_create_in_folder_test.dart` (`-d macos`, 실앱)
> 볼트: 임시 리다이렉트(실볼트 미접근) · 브랜치: `feat/pr-a-recovery-net` @ `108cc96`

## 결과 요약

| 시나리오 | 코드 assert | 비전 판정 | 종합 |
|---|---|---|---|
| S1 폴더 내 시크릿 생성 (클린 볼트 경로) | ✅ green (`All tests passed`, 00:14) | ✅ 3/3 PASS | **PASS** |

**⚠️ 정직 배너**: 이 PASS는 "클린 볼트 → 폴더 생성 → 명시 선택 → 생성" 경로에 한정된다.
사용자가 보고한 버그(현재 폴더 미생성 + 사이드바 집계 오류)는 **이 경로에서 재현되지 않았다** —
버그가 없다는 증명이 아니라, 재현 조건이 다르다는 신호다(아래 "재현 가설" 참조).

## 체크포인트 판정 (항목별)

**03_folder_selected** — 폴더 선택 직후:
- [PASS] Production 선택 하이라이트: 사이드바 FOLDERS 하단 Production 행에 녹색 액센트 텍스트 + 필 배경 + 좌측 보더, 카운트 0
- [PASS] 테이블: "this folder is empty" + 푸터 "0 secrets" (신규 빈 폴더 정상)
- [PASS] General=0, 카테고리(All/API/Tokens/Passwords/Certificates) 전부 0

**04_after_save** — 시크릿 저장 직후 (결정 컷):
- [PASS] 메인 테이블 첫 행에 "S1 GitHub Token" 표시 (NAME 컬럼)
- [PASS] Production 선택 하이라이트 유지 (녹색 필)
- [PASS] 사이드바 Production=1 (+1), General=0 (불변). All Keys=1, 푸터 "1 secret" 정합
- [관찰-a] "S1 GitHub Token"이 **API Keys=1**로 분류(Tokens=0) — 모달 기본 타입 추정. 결함 아닌 분류 정책 확인 필요
- [관찰-b] 저장 직후 상세 패널이 "select a secret" — 새 시크릿 자동 선택 안 됨 (UX 개선 후보)

**01_dashboard_initial · 02_folder_created**: 코드 레벨로 검증(General 시딩 표시·Production 트리 등장 —
`pumpUntil` 파인더 통과가 곧 assert). 비전 정밀 판정은 결정 컷 2장(03·04)에 수행.

- 캡처 원본: `~/Library/Containers/com.keybox.keyBox/Data/Library/Application Support/com.keybox.keyBox/vision_qa/s1/*.png` (2880x1736, 4장)
- 브레드크럼 항목 관련: **전용 브레드크럼/헤더 위젯은 앱에 존재하지 않음**(파인더 맵 ⑥b) — "현재 폴더" 판정은 사이드바 하이라이트로 대체(체크리스트 반영됨)

## 게이트 뮤테이션 실증

- **코드 assert 레이어**: Production 카운트 기대값을 1→2로 일부러 오염 → **RED 실증 완료** (`00:30 +0 -1: Some tests failed`) → 원복 후 analyze clean
- **비전 판정 레이어**: 기대값 "General=5"로 판정 시 화면 근거(General 옆 "0")와 불일치 → FAIL 판정 동작 확인
- 빌드 신선도: `flutter test integration_test -d macos`는 자체 빌드하므로 이 경로에선 스테일 불가능. mtime 게이트는 `.app` 직접 구동(dogfood) 경로에만 해당

## 재현 가설 — 사용자 보고 버그의 다음 사냥터

파인더 맵 ⑧(시크릿 folderId 결정 경로) 기반:

1. **폴더 미선택 폴백(⑧A)**: All Keys/카테고리 뷰에서 저장하면 `folders.first`(=General)로 배정되는 것이
   **설계 동작** — 사용자가 "현재 폴더"라고 인지한 것이 카테고리 뷰였다면 General행이 정상이며,
   이는 버그가 아니라 **UX 기대 불일치**(저장 모달에 대상 폴더 표시/선택 UI 부재)
2. **lock/unlock 후 provider 스테일**: 직전 커밋 `fed5da6`(폴더 provider lock/unlock 반응성 픽스)이
   이미 잡은 클래스일 가능성 — S3(잠금/해제 복원) 시나리오로 검증 필요
3. **스테일 빌드**: 사용자 실사용 앱이 수정 전 바이너리였을 가능성 (기존 교훈 재발 패턴)

**후속 (별도 승인)**: 관찰-a(타입 분류 정책) 확인.

---

# 2차 실행 — S1-variant · S3 (같은 날, 사용자 승인 후)

## S1-variant: 폴더 미선택 저장 → General 폴백 판별

- 테스트: `s1_variant_no_folder_test.dart` — **green** (`All tests passed`, 00:12)
- 코드 assert: All Keys 목록 표시 ✅ / General=1(폴백 실증) ✅ / Production=0(대조군 무오염) ✅ / General 열면 실제 포함 ✅
- 비전 판정(`02_after_save_no_folder`): 집계 전부 정합. **결정적 발견 — 화면 어디에도
  "General에 저장됨" 표시가 없음**: 테이블에 폴더 컬럼 없음, 저장 완료 토스트 없음,
  저장 모달에 대상 폴더 표시/선택 UI 없음. 무음 General 파일링.

**판별 결론**: "새 시크릿이 현재 폴더에 안 생기고 General로 감"은 — 폴더를 **선택한**
상태에선 재현 불가(S1 PASS), 카테고리 뷰(미선택)에선 **설계 동작**(folders.first 폴백,
`sheet_modal.dart` `_save`). 결함이 아니라 **가시성 부재로 인한 UX 기대 불일치**로 판별.

**개선 제안 (별도 승인 대상)**:
1. 저장 모달에 대상 폴더 셀렉터 추가(기본값 = 현재 선택 폴더, 미선택 시 General 명시 표시)
2. 최소안: 저장 직후 "Saved to <폴더>" 토스트 또는 테이블 폴더 컬럼

- 뮤테이션: General 기대 1→2 오염 → **RED 실증**(`00:28 +0 -1: Some tests failed`) → 원복

## S3: lock → unlock → 상태 복원 (fed5da6 계약 검증)

- 테스트: `s3_lock_unlock_restore_test.dart` — **green** (`All tests passed`, 00:18)
- **잠금 경로**: Cmd+L 키 시뮬레이션이 실앱에서 실동작(`[VISION_QA] lock path used: cmd+L`)
  — 실사용자 경로 그대로 주행, provider 폴백 불사용
- 코드 assert: 잠금 화면 도착 ✅ / 해제 후 트리에 General·Production ✅ /
  Production 카운트=1 재로드 ✅ / 선택 폴더 목록에 시크릿 재표시 ✅
- 비전 판정(`03_after_unlock`): 트리·카운트·선택 하이라이트·"1 secret" 푸터 전부 정합,
  스테일/빈 트리 증상 없음. "auto-lock in 14m" 재무장 표시 확인
- [관찰-c] 잠금 전 라이트(Bench) → 해제 후 다크(Terminal) 테마 플립 — first-run hero가
  첫 잠금에서 clear되는 **설계 동작**(theme_provider). 결함 아님. 단 "잠금 후 테마가
  바뀌었다"는 체감 가능성 있음 → UX 판단은 사용자 몫으로 기록
- 뮤테이션: 해제 후 Production 기대 1→2 오염 → **RED 실증**(`00:29 +0 -1: Some tests failed`) → 원복·analyze clean

## 최종 판별 — 사용자 보고 버그

| 가설 | 결과 |
|---|---|
| ① 폴더 미선택 General 폴백 | **채택** — 설계 동작 + 무음 파일링 UX 갭(S1V 실증). 개선 제안 2건 위 참조 |
| ② lock/unlock provider 스테일 | **기각** — fed5da6 계약이 실앱에서 완결(S3 실증) |
| ③ 스테일 빌드 | 판별 불가(과거 시점) — 재발 시 dogfood.sh 신선도 게이트로 즉시 판별 가능 |

**사이드바 집계**: S1·S1V·S3 세 시나리오 모두에서 집계 정합 — 현재 코드에서 집계 결함
증거 없음. 재발 시 해당 상태를 시나리오로 재현해 추가할 것.
