<% content_for(:title, "New Secret — KeyBox") %>

<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-2xl font-bold text-[var(--text-primary)] mb-6">New Secret</h1>
  <%= render "form", secret: @secret %>
</div>
