<% content_for(:title, "New Folder — KeyBox") %>

<div class="max-w-lg mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-2xl font-bold text-[var(--text-primary)] mb-6">New Folder</h1>
  <%= render "form", folder: @folder %>
</div>
